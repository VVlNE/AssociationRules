<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/absoluteWhite"
    android:paddingTop="5dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingBottom="5dp">

    <Button
        android:id="@+id/significanceMeasuresButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="@string/significance_measures_button"
        android:background="@drawable/button_selector"
        style="?android:attr/borderlessButtonStyle"
        android:textStyle="bold" />

    <Button
        android:id="@+id/recommendationButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="@string/recommendation_button"
        android:background="@drawable/button_selector"
        style="?android:attr/borderlessButtonStyle"
        android:textStyle="bold" />

</LinearLayout>