<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.vvine.associationrules">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LightTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/main_activity_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FileOpeningActivity" />

        <activity
            android:name=".SignificanceMeasuresActivity"
            android:label="@string/significance_measures_activity_name" />

        <activity
            android:name=".MoreDetailsActivity" />

        <activity
            android:name=".RecommendationActivity"
            android:label="@string/recommendation_activity_name" />

        <activity
            android:name=".TwoElementsSignificanceMeasuresActivity"
            android:label="@string/two_elements_significance_measures_activity_name"/>
    </application>

</manifest>